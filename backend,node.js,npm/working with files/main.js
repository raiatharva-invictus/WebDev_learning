const { error } = require("console");
const fs = require("fs")

// console.log(fs); // basically you will get all function you can use of fs

// console.log("starting");
// fs.writeFileSync("atharva.txt", "Atharva is a good boy") // after this is done it will move to next line only then
// console.log("ending");

console.log("starting");
// fs.writeFile("atharva2.txt", "Atharva is a good boy 2", ()=>{ // writeFile will be scheduled to run 
//     console.log('done'); // this will be logged at last
    
// }) 

// say we also want to read a file
// fs.writeFile("atharva2.txt", "Atharva is a good boy 2", ()=>{ // writeFile will be scheduled to run 
//     console.log('done'); // this will be logged at last
//     fs.readFile("atharva2.txt", (error, data)=>{
//         console.log(error, data.toString());
//     })
// }) 
// fs.appendFile("atharva.txt", " atharva is a good boy again saying that hehe", (e,d)=>{
//     console.log(d);
// })
// console.log("ending");


