import fs from "fs/promises"

// let a = await fs.readFile("atharva.txt") // since this is a module we can directly await here
// let b = await fs.writeFile("atharva.txt", "\n\n\n\n\n\n\n\nthis is written") // since this is a module we can directly await here
// console.log(a.toString());
// console.log(a.toString(),b);

// let c = await fs.appendFile("atharva.txt", " kya hua hai")