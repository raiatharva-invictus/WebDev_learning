<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FlexBox</title>
  <style>
    /* theory - flexbox is used for easy development of layouts in modern web development. 
    we start by setting parent properties that are:
    1) most imp is setting display: center;
    2) we use flex-direction: row; several are row-reverse, column, column-reverse
    3) we use justify-contents: center; this works on main axis and has several option like flex-end, flex-start, etc.
    4) we use align-items: center; this works on cross axis and has several option like flex-end, flex-start, etc. this is used when we are working with single line of items for multiple line we use below property
    5) align-content: center; it has several other properties
    5) flex-wrap 
    6) flex-flow is used to set flex-direction flex-wrap
    7) gap property gap: <row> <column>; , row-gap: ; , column-gap: ;

      child properties:
    1) order: ; this sets the order of selected item , which by default for all is 0 , we can set value that can be negative positive depending on where we want the element to be
    2) flex-grow: ;
    3) flex-shrink: ;
    4) align-self: ;

    */
    .container{
      border: 2px solid black;
      display: flex;
      height: 80vh;
      /* flex-direction: row-reverse; */  
      /* justify-content: center; */
      /* align-items: center;   */
      /* align-content: center; */
      /* there is no justify-items in flexbox */
      /* flex-wrap: wrap; */
      /* flex-flow: row wrap; */
      /* gap: 10px; */
      /* row-gap: 10px;
      column-gap: 10px; */
      gap: 10px 20px;
    }
    .item{
      /* flex-grow: 1; */
      width: 92px;
      height: 92px;
      border: 2px solid black;
      background-color: blueviolet;
      /* margin: 4px; */
    }
    /* .order1{
      order: -1;
    }
    .order2{
      order: 2;
    } */

    .item1{
      /* flex-grow: 2; */
      flex-shrink: 2;
      /* align-self: flex-end; */
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="item item1">1</div>
    <div class="item">2</div>
    <div class="item">3</div>
    <div class="item order1">4</div>
    <div class="item">5</div>
    <div class="item order2">6</div>
    <div class="item">7</div>
    <div class="item">8</div>
    <div class="item">9</div>
    <div class="item">10</div>
  </div>
</body>
</html>