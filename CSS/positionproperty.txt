<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Position Property</title>
  <style>

    /* some basic position properties */
    /* position:relative; -> 
    position:absolute; -> this sets the top, bottom, left, right according to the nearest positioned element when none exist near it takes html as nearest positioned.
    position:fixed; -> makes the target fixed in such a way that even after scrolling page it stays at the set top left bottom right
    position:sticky; -> makes the target stick if it scroll up or say the scroll tries to hide the target */

    *{
      margin: 0;
      padding: 0;
    }
    .box{
      height: 300px;
      width: 80vw;
      border: 2px solid black;
      margin: 5px;
      padding: 2px;
    }
    .box1{
      /* position: relative;
      z-index: 1; */
      position: absolute;
      top: 0px;

      /* position: sticky;
      top: 0px;
      height: 70px;
      width: 100%;
      margin: 0;
      padding: 0; */

      background-color: aqua;
    }
    .box2{
      /* top: -34px;
      left: 24px;
      position: relative; */
      background-color: antiquewhite;
    }
    .box3{
      /* bottom: 0px;
      height: 100px;
      right: 12.2vw;
      position: fixed; */
      background-color: azure;
    }
    .box4{
      background-color: bisque;
    }
    .parent{
      padding: 43px;
      margin: 34px;
      border: 2px solid red;
      /* Important Exception : transform, filter, or perspective can also make an element appear as positioned that is even if you don't make it position, these 3 properties can make it position causing us bug since we wont be knowing what is making position */
      /* position: absolute; */
      /* transform: translate(0); */
      /* filter: invert(); */
      /* perspective: 0em; */
    }
  </style>
</head>
<body>
  <div class="parent">
    <div class="box box1"></div>
    <div class="box box2"></div>
    <div class="box box3"></div>
    <div class="box box4"></div>
  </div>
</body>
</html>